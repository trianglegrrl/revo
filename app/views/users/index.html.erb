<p id="notice"><%= notice %></p>

<h1>Check In Now!</h1>

<table>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td class="check_in_user_list"><%= link_to user.full_name, user_user_action_steps_path(user) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Member', new_user_path %>
